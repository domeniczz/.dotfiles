exec amixer sset Master 0%
exec brightnessctl set 100%

exec udiskie &
exec mako &
exec wl-paste --watch cliphist store
exec fcitx5 -d
exec foot --server
exec sh -c 'rm -f /tmp/volume-wob-pipe && mkfifo /tmp/volume-wob-pipe && tail -f /tmp/volume-wob-pipe | wob'

exec sh -c 'sleep 0.5 && swaymsg "workspace 2; exec $terminal"'

# exec swayidle -w \
#     timeout 1800 'brightnessctl -s set 5%' resume 'brightnessctl -r' \
#     timeout 3000 'systemctl suspend' \
#     before-sleep 'swaylock'
